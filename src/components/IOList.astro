---
const { io } = Astro.props
---

<table class="w-full mx-auto">
  <tbody>
    {
      Object.entries(io).map(([name, desc]) => (
        <tr class="border-b border-slate-200">
          <td>{name}</td>
          <td>{desc}</td>
        </tr>
      ))
    }
  </tbody>
</table>
